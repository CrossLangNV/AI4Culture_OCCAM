version: '3'

services:
#  notebook:
#    build:
#      context: .
#      dockerfile: Dockerfile
##    env_file:
##      - .env
#    ports:
#      - 8888:8888
#    volumes:
#      - ./evaluation:/lab/evaluation
#    command: jupyter-lab --port=8888 --ip=0.0.0.0 --no-browser --allow-root #--NotebookApp.token=''

  api:
    build:
      context: .
      dockerfile: app.dockerfile
    image: docker.crosslang.com/ai4europeana/text-segmentation-api
    ports:
      - 17100:80
    env_file:
      - app/.env
